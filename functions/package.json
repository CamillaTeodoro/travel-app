{
  "name": "travel-app-functions",
  "description": "Cloud Functions do TravelQuiz (recomendações de destino por IA)",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "echo \"(sem testes ainda — chegam na Etapa 4 com firebase-functions-test)\" && exit 0"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.1"
  },
  "devDependencies": {
    "typescript": "~5.6.3"
  }
}
